/// Approximate USD→INR used for display when `priceInr` is missing or for coupon conversion.
/// Not a live FX rate; replace with an API/cached rate when product provides both currencies from server.
const double kUsdToInrDisplay = 83.0;
